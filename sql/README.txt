This directory will just contain all the SQL files we create for the database needed in the project. 

The SQL files should be named the number of the database patch so we can keep order; the first should be 1.sql, the second 2.sql, and so forth. This way we can run patches in order.
